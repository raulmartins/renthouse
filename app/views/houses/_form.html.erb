<%= form_for(@house) do |f| %>
  <% if @house.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@house.errors.count, "error") %> prohibited this house from being saved:</h2>

      <ul>
      <% @house.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :typehouse %><br>
    <%= f.text_field :typehouse %>
  </div>
  <div class="field">
    <%= f.label :compartment %><br>
    <%= f.number_field :compartment %>
  </div>
    <div class="field">
    <%= f.label :status %><br>
    <%=f.select( "status", %w(Alugado Livre), { include_blank: "Selecione" })%>
  </div>
    <%= f.fields_for :address  do |a|%>
  <div class="field"> 
    <%= a.label :street %><br>
    <%= a.text_field :street %>
  </div> 
  <div class="field">
    <%= a.label :number %><br>
    <%= a.text_field :number %>
  </div>
  <div class="field">
    <%= a.label :zip %><br>
    <%= a.text_field :zip %>
   </div> 
  <%end%>
  <div class="field">
    <%= f.label :obs %><br>
    <%= f.text_area :obs %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
